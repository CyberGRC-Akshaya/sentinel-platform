services:
  backend:
    build:
      context: ./backend
    container_name: sentinel-backend
    ports:
      - "8000:8000"

  frontend:
    build:
      context: ./frontend
    container_name: sentinel-frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend